参考当前目录所有图片 展示的是视频节点的样式

这里的裁剪可以参考 packages\canvas-core\src\nodes\image 中的裁剪方案 效果是一样的 
这里的视频剪辑 则是使用的 底部 状态栏（自定义）

还有这个视频节点的UI，当前ui 中间的图标按钮有一个背景阴影，请删除他

然后就是 他提供下载和全屏（顶部toolbar），全屏可以参考  packages\canvas-core\src\nodes\panorama中的全屏实现

其次就是在节点中实现 自定义的UI 而不是使用 video 默认的控件，注意右下角支持截图 首尾帧（截图之后自动创建一个新的图片节点 可以参考 packages\canvas-core\src\nodes\image 图片裁剪之后生成新的图片节点）

这里的视频剪辑和裁剪也同样需要在画布中生成新的视频节点

请全程自动化，使用 /using-superpowers进行全流程操作， 自行判断每个流程使用技能，同时多使用使用子代理， 

完成之前需要自己使用 浏览器进行检查， 检查UI，检查操作是否可以正常操作，让功能跑起来

同时注意UI的样式保持一致性
