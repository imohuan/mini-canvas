# 2026-06-18

## 12:10 — SelectionFrame: 修复 group 子节点选框位置错误

**Bug**: `SelectionFrame.vue` 的 `rawSelectionBounds` 计算（第 96-97 行）用 `node.position`（相对父 group 坐标）计算选中节点边界框，导致 group 内子节点被选中时选框位置偏移。

**Fix**: `SelectionFrame.vue:96-97` → `node.position.x/y` 替换为 `node.computedPosition?.x ?? node.position.x`

**Previous fix (22:50)**: `MultiSelectPlugin.ts:124-125` 同样的坐标系修正用于碰撞检测

**Verification**: vue-tsc + vite build passed. 1 file, 2 lines.
