# 2026-06-28 Daily Log

## Unified ResourceItem 重构 (prosemirror-editor-bundle)

将 `resources` + `variables` 双轨系统合并为单一 `ResourceItem[]`，每个 item 自带 `renderItem(item)` 自定义渲染逻辑。

### 变更文件 (7 tasks, Subagent-Driven)
- `types.ts`: 删除 `VariableItem`，`ResourceItem` 新增 `category`, `icon`, `renderItem`, `mediaType`, `value`
- `useEditor.ts`: Schema 合并（删除 variable node），双轨逻辑删除（单 filteredItems + groupedItems），moveUp/moveDown 简化为纯索引运算
- `MentionMenu.vue`: 动态 category 分组，`item.renderItem(item)` 自定义渲染，全局索引导航
- `index.vue`: 单 `resources` prop，无 `@variable-insert`
- `demo/App.vue`: 合并变量/资源为单一数组，每项带 renderItem
- `utils.ts`: `resource.type` → `resource.mediaType`，url 可选 guard
- `web-component.ts`: 同 type → mediaType

### 构建状态
- TypeScript: 零残留引用（VariableItem/filteredVariables/filteredResources/menuType 全部清除）
- Vite root build: ✓
## ImageBottomToolbar 接入连接节点素材

- 使用 `useVueFlow` + `NodeIdInjection` 获取上游连接节点
- `connectedImages` computed：过滤 edges 中 target 为本节点的，提取 sourceNode.data.imageUrl/imageName
- 构造 ResourceItem[] 传给 ProseMirrorEditor `:resources`
- 每个素材带 renderItem（菜单缩略图）、renderEditor（输入框缩略图）、onClick（内联简易全屏预览）

- `ResourceItem.renderEditor?: (item) => HTMLElement` — 替代 toDOM 的自定义输入框内渲染
- `toDOM` + `nodeView` 并存：toDOM 供 ProseMirror 内部用（剪贴板），nodeView 走 renderEditor
- `itemRegistry` 全局 Map：NodeView 通过 attrs.id 查找 resourceItem，调用 renderEditor
- **Bug 修复**: itemRegistry.set 必须在 view.dispatch 之前（否则 NodeView 触发时查不到）

## MentionMenu hover vs 键盘选择分离

- hover 只改局部 `hoveredIndex`（只变背景色），不触发 scroll
- keyboard 才改 `activeIndex` → watch 触发 scrollToActiveItem
- 移除 `handleMenuHover` 死代码
