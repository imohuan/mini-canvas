# 2026-06-27 工作日志

## 修复：拖拽连接线时 BaseNode 递归更新错误

**错误**：`Maximum recursive updates exceeded in component <BaseNode>` — 拖拽连接线时 Vue 检测到递归更新超过上限。

**根因**：`useCanvasConnection.ts` 的 `buildConnectionEdgeProps` 函数在 Vue render 阶段写入 `canvas.connectionState.hoverNode`（Pinia reactive state）。该函数在 `<template #connection-line>` 插槽中调用，属于 render 阶段。写入触发的 reactive 依赖传播回自身，形成 `render → write → re-render → ...` 递归链。

**修复（两处）**：

1. **`useCanvasConnection.ts:780-794`** — 将 `hoverNode` 写入包装在 `nextTick` 中，迁出 render 阶段：
   ```ts
   if (hoverChanged) {
     const nextHover = ... // 捕获快照值
     nextTick(() => { canvas.connectionState.hoverNode = nextHover })
   }
   ```

2. **`BaseNode.vue:273-306`** — `cardInlineStyle` 从 `computed`（每次返回新对象）改为 `shallowRef` + `watch`（值变化时才更新引用），减少不必要的样式重绘。放置位置在 `cardTransform` 定义之后，确保依赖的 `cardWidth`、`cardHeight`、`zoom`、`cardTransform` 已声明。

## 实现：图片扩展（Outpaint）功能 — 重构

### 架构决策
- **Expand container 用 `Teleport :to="viewportRef"`**（非 body），`position: absolute; inset: 0`。与 NodeToolbar 同层 stacking context，backdrop z-index=0，toolbar 正常可见
- **所有 rect 收敛到 `_overlay` 内**：`_cropRect`/`_expandRect` 从 node.data 顶层迁入 `CanvasNodeOverlay`
- **Storage 双重清理**：`sanitizeForSave` + `loadCanvas` 都 strip `_overlay`，防止刷新后残留裁剪/扩展模式

### 改动文件
| 文件 | 改动 |
|------|------|
| `CanvasNodeData.ts` | `CanvasNodeOverlay` 新增 `_cropRect`/`_expandRect`；`CropRect` 改为 `Rect` 别名 |
| `ImageExpander.vue` | 全量重写：viewportRef, ToolbarButton 渲染 registry group:'expand' |
| `ImageNode.vue` | `onCropUpdate`/`onExpandUpdate` 写入 `_overlay` 子字段 |
| `ImageNodePlugin.ts` | 所有 rect 操作在 `_overlay` 内；cancel/confirm 删除只清 `delete data._overlay` |
| `BaseNode.vue` | `showSelectionOutline` computed：`_overlay` 存在时隐藏选中边框 |
| `StoragePlugin.ts` | `loadCanvas` 加载时 strip `_overlay` 及 legacy rect 字段 |
| `sanitizeForSave.ts` | RUNTIME_FIELDS 新增 `_overlay`, `_expandRect`, `_expandMode` |
| `Canvas.vue` | 清理 legacy `_cropRect`/`_cropMode` 删除代码（`delete _overlay` 已涵盖） |

