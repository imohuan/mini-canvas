# 2026-06-20

## 中文乱码修复（6个文件）
- **问题**: BaseNode.vue, CustomEdge.vue, useCanvasFlow.ts, useCanvasShortcuts.ts, useCanvasStore.ts, SelectionFrame.vue 的中文注释/字符串因文件双重编码损坏（UTF-8 字节被当作其他编码解释后重存），出现 `鏍囬` 等乱码
- **根因**: 文件在某个环节（可能是编辑器保存或 git checkout）以非 UTF-8 编码写入，导致中文内容永久损坏
- **修复策略**: 从 git HEAD 恢复具有正确中文内容的版本，然后应用 `state` 重构的代码变更（`canvas.state.XXX` → `canvas.state.core.XXX`）
- **关键变更**:
  - 5 个简单文件：git HEAD 恢复 + 全局 `canvas.state.` → `canvas.state.core.` 替换
  - useCanvasStore.ts：手动重构 `state` 结构（扁平 → `core:` 嵌套），同步更新 serializer 支持新旧两种持久化格式的兼容
