## canvas-core 包质量审计 (fix-canvas-core-stability) — 2026-07-04

### 审计方式
codegraph 索引 (2382 nodes, 6838 edges) + grep + 直接代码阅读，对 canvas-core 包做全面质量审计。

### 已修复项（本分支 30 commits）
P0 全部修复：EventBus 泄漏 ×8、buildConnectionEdgeProps DOM 瓶颈、ImageCropper/Expander/Masker onUnmounted、beforeunload sanitizeForSave、panoUrl RUNTIME_FIELDS、performSave 竞态、createStubContext 死代码。

### 本轮修复（2026-07-04 第二轮）
- ✅ P1: 删除 TypedEventBus 死代码（零调用者）
- ✅ P1: 提取 useImageDisplay composable，消除 ImageCropper/Expander/Masker 中重复的 object-contain 计算
- ✅ P2: 删除 BaseNode.vue 重复 clamp 定义
- ✅ P2: 提取 useUpstreamImages composable，消除 ImageCompareNode/ImageBottomToolbar/PanoramaNode 中重复的 connectedImages 管道
- ✅ P2: 移除 MovingHandle.vue:98 console.log 残渣
- ✅ P2: 移除 ImageBottomToolbar test-fallback 测试数据
- ✅ P2: 修复 PanoramaNode textureLoader 内存泄漏（module-level → init 创建/destroy 清除）
- ✅ P2: 提取 BaseNode.vue connect feedback 常量到 constants.ts
- ✅ P2: 清理 BaseNode.vue 重复 JSDoc 注释
- ✅ P3: 移除 CanvasNodeData.ts deprecated CropRect 类型
- ✅ P3: 移除 ImageBottomToolbar.vue 注释代码

vue-tsc --noEmit 零错误通过。

### 未修复（性能优化不在本次范围）
- findNearestValidTarget 每帧 DOM 查询（需要 ResizeObserver 缓存方案）
- 散布的 console.log（38 处在生产代码中，需逐文件评估哪些是必要的 debug 日志）

### 额外修复（pnpm build 校验中暴露）
- ✅ 添加 sanitizeForSave.ts 缺失的 RUNTIME_FIELD_SET 导出（StoragePlugin.ts 运行时崩溃）
- ✅ 修复 Canvas.vue:145 Unicode 模板字符串损坏
- ✅ 修复 CustomNode.vue:29 Unicode 字符串损坏 + 缺失 defineEmits('updateNodeInternals') 消除 Vue 警告
- ✅ 修复 useCanvasConnection.ts:24 残留合并标记 $1
- ✅ 修复 MovingHandle.vue 缺失 clamp 导入

### 样式修复（is-connection-invalid 禁用态统一）
- ✅ BaseNode.vue: 去掉红色边框 + 移除 content-clip filter，改用 ::after + backdrop-filter 统一遮罩（502482c）

### 多轮 commit
```
ec8b08b fix(canvas-core): repair runtime crashes in connection line and context menu
0543a77 refactor(canvas-core): extract shared composables for image display and upstream nodes
36ed8ff refactor(canvas-core): remove dead code and clean up cruft
```
共 3 轮，18 个文件变更，剩余仅 .codegraph/.gitignore 未纳入（pre-existing）。
