/** 节点默认卡片尺寸（宽 x 高，像素） */
export const DEFAULT_NODE_SIZE = {
  width: 256,
  height: 256,
} as const

/** 性能面板默认节点估算尺寸 */
export const DEFAULT_PERF_NODE_SIZE = {
  width: 180,
  height: 120,
} as const
